#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * Return: 0 is a success
 */

int main(void)

{
	char c;

	for (c = 'a'; c < = 'z' ; c++)
		putchar (c);
	putchar('\n');
	return (0);
}
