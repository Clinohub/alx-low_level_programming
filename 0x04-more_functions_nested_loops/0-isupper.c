include "main.h"

/**
 * _isupper - this is function that checks for uppercase character.
 * @c: an integer
 * Return: 1 if c is uppercase 0 otherwise
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z');
}
